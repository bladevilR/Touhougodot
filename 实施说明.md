# 竹林小屋 - 系统实施说明文档

## 📋 已完成的工作

### 1. 核心系统文件（已生成）

#### ✅ 人性系统
**文件**: `scripts/core/autoloads/HumanitySystem.gd`
- 管理0-100的人性值
- 各种行为触发器（睡觉+2，喝茶+1.5，吃饭+2.5，昏倒-10等）
- 阈值检测（20以下触发灵梦退治，40以下慧音关心）
- 完整的存档支持

#### ✅ 疲劳系统
**文件**: `scripts/core/autoloads/FatigueSystem.gd`
- 管理0-100的疲劳值
- 自动累积：白天活动2.5/小时，战斗1/分钟，农业3.5/小时
- 三个警告等级：60疲惫，80精疲力竭，100强制昏倒
- 过度疲劳会减少人性，并强制传送回家睡觉
- 完整的存档支持

#### ✅ NPC日程系统
**文件**: `scripts/core/autoloads/NPCScheduleManager.gd`
- 管理NPC的每日日程
- 预设了慧音和灵梦的完整日程
- 自动响应时间变化
- 支持特殊条件触发（如人性过低时灵梦的退治日程）
- 完整的存档支持

#### ✅ 同伴系统
**文件**: `scripts/core/autoloads/CompanionSystem.gd`
- 管理同伴招募和跟随
- 最多1个同伴同时跟随
- 同伴战斗属性系统
- 检查NPC日程冲突
- 完整的存档支持

#### ✅ 小屋交互系统
**文件**: `scripts/systems/home/HomeInteractionSystem.gd`
- 睡觉功能（恢复疲劳+增加人性+推进时间���
- 喝茶功能（+1.5人性，-5疲劳）
- 吃饭功能（+2.5人性，-10疲劳）
- 读书功能（+1人性，+2疲劳）
- 存储箱系统（200格独立存储）
- 完整的存档支持

### 2. 修改的现有文件

#### ✅ SignalBus.gd
新增信号：
- 人性系统：`humanity_changed`, `humanity_threshold_crossed`, `humanity_warning`
- 疲劳系统：`fatigue_changed`, `fatigue_warning`, `player_collapsed`
- NPC日程：`npc_schedule_changed`, `npc_arrived_at_location`
- 羁绊系统：`bond_level_changed`, `bond_points_changed`, `companion_unlocked`, `bond_event_triggered`
- 同伴系统：`companion_recruited`, `companion_joined_party`, `companion_left_party`, `companion_hp_changed`
- 小屋交互：`sleep_started`, `sleep_completed`, `home_storage_opened`, `home_storage_closed`, `tea_interaction_completed`, `meal_interaction_completed`, `read_book_completed`

#### ✅ GameStateManager.gd
- 新增游戏模式：`HOME`（在家）、`SLEEPING`（睡眠中）
- 更新 `can_player_move()` 和 `can_open_menu()` 逻辑
- 更新 `_mode_to_string()` 函数

#### ✅ SaveSystem.gd
- 版本升级到 2.0.0
- 集成所有新系统的存档数据
- `_collect_save_data()` 添加：humanity, fatigue, npc_schedules, companions, home
- `_apply_save_data()` 添加所有新系统的读档逻辑

---

## ⚠️ 待完成的工作

### 关键任务（必须完成才能运行）

#### 1. 修改 project.godot
**文件**: `project.godot`

需要添加以下自动加载配置（在现有autoload部分）：

```ini
[autoload]
# ... 现有的自动加载 ...

HumanitySystem="*res://scripts/core/autoloads/HumanitySystem.gd"
FatigueSystem="*res://scripts/core/autoloads/FatigueSystem.gd"
NPCScheduleManager="*res://scripts/core/autoloads/NPCScheduleManager.gd"
CompanionSystem="*res://scripts/core/autoloads/CompanionSystem.gd"
HomeInteractionSystem="*res://scripts/systems/home/HomeInteractionSystem.gd"
```

**重要**：确保这些在已有的 TimeManager、CalendarManager 之后添加，因为新系统依赖它们。

#### 2. 创建竹林小屋场景
**文件**: `scenes/home/BambooHouse.tscn`

最小可行版本需要：
- 一个 Node2D 根节点
- 一个简单的背景 ColorRect（棕色或绿色代表竹林）
- 几个交互区域（Area2D）：
  - 床铺区域（睡觉）
  - 茶桌区域（喝茶）
  - 餐桌区域（吃饭）
  - 存储箱区域（存储）
- 玩家生成点

**参考脚本**：
```gdscript
# BambooHouse.gd
extends Node2D

func _ready():
	name = "BambooHouse"
	GameStateManager.change_mode(GameStateManager.GameMode.HOME)
	print("[BambooHouse] 进入竹林小屋")
```

#### 3. 创建交互区域脚本
**文件**: `scripts/systems/home/SleepArea.gd`

```gdscript
extends Area2D

var player_in_range: bool = false

func _ready():
	body_entered.connect(_on_body_entered)
	body_exited.connect(_on_body_exited)

func _on_body_entered(body):
	if body.name == "Player":
		player_in_range = true

func _on_body_exited(body):
	if body.name == "Player":
		player_in_range = false

func _process(_delta):
	if player_in_range and Input.is_action_just_pressed("interact"):  # E键
		HomeInteractionSystem.interact_sleep()
```

类似地创建：
- `TeaArea.gd` → 调用 `HomeInteractionSystem.interact_tea()`
- `MealArea.gd` → 调用 `HomeInteractionSystem.interact_meal()`
- `StorageArea.gd` → 调用 `HomeInteractionSystem.open_storage()`

#### 4. 修改主场景（可选，但推荐）
**文件**: `project.godot`

```ini
[application]
run/main_scene="res://scenes/home/BambooHouse.tscn"
```

将主场景从 CombatArena 改为 BambooHouse。

---

## 🔧 测试步骤

### 1. 启动测试
1. 确保 project.godot 已添加所有自动加载
2. 运行游戏
3. 检查控制台是否有系统初始化消息：
   ```
   [HumanitySystem] 人性系统已初始化，当前人性: 50.0
   [FatigueSystem] 疲劳系统已初始化
   [NPCScheduleManager] NPC日程系统已初始化
   [CompanionSystem] 同伴系统已初始化
   [HomeInteractionSystem] 小屋交互系统已初始化
   ```

### 2. 测试人性系统
在控制台或脚本中：
```gdscript
HumanitySystem.modify_humanity("drink_tea")  # 应增加1.5
print(HumanitySystem.current_humanity)  # 应显示51.5
```

### 3. 测试疲劳系统
```gdscript
FatigueSystem.add_fatigue(50)  # 增加50点疲劳
# 应在控制台看到："提示：感到疲惫"
```

### 4. 测试睡眠
如果已创建 BambooHouse 场景和 SleepArea：
1. 进入床铺区域
2. 按E键
3. 应看到：
   - 疲劳清零
   - 人性+2
   - 时间跳到次日6:00
   - 显示"新的一天开始了！"

### 5. 测试NPC日程
```gdscript
print(NPCScheduleManager.get_npc_location("keine"))  # 应根据时间显示位置
```

### 6. 测试存档
```gdscript
SaveSystem.save_game(1)  # 保存到槽位1
SaveSystem.load_game(1)  # 读取槽位1
# 检查人性、疲劳、NPC日程等是否正确恢复
```

---

## 🎮 核心游戏循环

系统已就绪后，游戏循环如下：

```
1. 玩家在竹林小屋醒来（HOME模式）
   ↓
2. 可选活动：
   - 喝茶（+1.5人性，-5疲劳）
   - 吃饭（+2.5人性，-10疲劳）
   - 整理存储箱
   ↓
3. 出门活动（切换到OVERWORLD模式）
   - 前往城镇、农场等
   - 疲劳每小时+2.5
   ↓
4. 可选：进入地下城战斗（COMBAT模式）
   - 疲劳每分钟+1
   ↓
5. 疲劳累积：
   - 60：感到疲惫（提示）
   - 80：精疲力竭（每小时-3人性）
   - 100：强制昏倒（-10人性，传送回家）
   ↓
6. 返回竹林小屋
   ↓
7. 睡觉（SLEEPING模式）
   - 疲劳清零
   - 人性+2
   - 时间跳到次日6:00
   - 所有NPC日程重置
   ↓
8. 新的一天开始，回到步骤1
```

---

## 📊 数值平衡

### 人性值
- 初始：50
- 每日睡觉：+2
- 每日喝茶/吃饭：约+4~+6
- 跳过睡眠：-5
- 昏倒：-10
- 预计每天净增长：0~+3（正常游玩）

### 疲劳值
- 初始：0
- 每日累积：约20~40（根据活动强度）
- 睡觉恢复：完全清零
- 建议每游戏日睡一次

### NPC日程
- 慧音：主要在寺子屋（8-17点），晚上在家（19-22点）
- 灵梦：主要在神社，下午巡逻城镇（14-17点）

---

## 🐛 已知问题和注意事项

### 1. TimeManager 依赖
- FatigueSystem 依赖 TimeManager 的 `hour_changed` 信号
- 确保 TimeManager 已正确配置并运行

### 2. 场景切换
- 疲劳昏倒时会强制切换场景到 BambooHouse
- 需要确保 BambooHouse.tscn 存在且路径正确

### 3. 玩家节点
- 系统假设玩家节点名为 "Player"
- 如果名称不同，需要修改交互区域脚本

### 4. UI未实现
- 当前没有UI显示人性和疲劳值
- 建议创建简单的HUD显示这些数值

---

## 🚀 下一步开发建议

### 短期（完成最小可行版本）
1. ✅ 创建简单的 BambooHouse 场景
2. ✅ 创建交互区域（床、茶桌、餐桌、存储箱）
3. ✅ 测试睡眠循环
4. ✅ 创建简单的HUD显示人性和疲劳

### 中期（丰富游戏内容）
1. 创建慧音和灵梦的NPC预制体
2. 实现NPC在场景中的实际移动
3. 实现对话系统与羁绊系统的连接
4. 创建羁绊UI界面
5. 实现同伴跟随逻辑

### 长期（完整系统）
1. 实现灵梦退治剧情
2. 实现慧音关心剧情
3. 添加更多NPC
4. 完善同伴战斗AI
5. 创建更多人性增长/减少的触发点

---

## 📝 代码使用示例

### 在其他脚本中使用新系统

```gdscript
# 增加人性
HumanitySystem.modify_humanity("help_npc")

# 检查人性状态
if HumanitySystem.current_humanity < 20:
	print("人性极低！")

# 增加疲劳
FatigueSystem.add_fatigue(10)

# 部分休息
FatigueSystem.rest_recovery(15)

# 检查NPC位置
var location = NPCScheduleManager.get_npc_location("keine")
if location == "temple_school":
	print("慧音正在寺子屋教书")

# 招募同伴
CompanionSystem.recruit_companion("keine")
CompanionSystem.add_to_party("keine")

# 小屋交互
HomeInteractionSystem.interact_tea()
HomeInteractionSystem.store_item("iron_ore", 50)
HomeInteractionSystem.retrieve_item("iron_ore", 10)
```

---

## 📞 需要帮助？

如果遇到问题：
1. 检查控制台错误信息
2. 确认所有自动加载已正确配置
3. 验证文件路径是否正确
4. 确保TimeManager和CalendarManager正常运行

---

## ✅ 完成检查清单

- [ ] project.godot 已添加5个新的自动加载
- [ ] 游戏启动时能看到所有系统初始化消息
- [ ] 创建了 BambooHouse.tscn 场景
- [ ] 创建了至少一个交互区域（床铺）
- [ ] 测试睡眠功能正常（疲劳清零、人性+2、时间推进）
- [ ] 测试存档功能正常（保存和读取）
- [ ] 控制台没有错误信息

全部完成后，基础系统即可正常运行！

---

**生成时间**: 2026-01-10
**版本**: 2.0.0
**状态**: ✅ 核心系统已完成，等待场景创建和project.godot配置
